{
  "framework": "sveltekit",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.svelte-kit/output",
  "installCommand": "cd frontend && npm install",
  "regions": ["sin1"],
  "rewrites": [
    { "source": "/(.*)", "destination": "/frontend/$1" }
  ]
} 